id: N04-release-readme-refresh
fixture: release-cli
prompt: "根据 CLI 源码更新 README 的命令示例并运行测试。"
mode: yolo
fakeScenario: release-readme
budgets: { referenceLatencyMs: 120000, referenceTokens: 18000, referenceToolCalls: 14 }
assertions:
  - type: file-matches
    path: README.md
    pattern: "release[\\s\\S]*validate|validate[\\s\\S]*release"
  - type: command
    command: node --experimental-strip-types --test
    expectedExitCode: 0
